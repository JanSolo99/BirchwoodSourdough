# Birchwood Sourdough - Environment Configuration
# Copy this file to .env (for local development) or set these variables in your Netlify dashboard

# REQUIRED: Airtable Configuration
AIRTABLE_API_KEY=your_airtable_api_key_here
AIRTABLE_BASE_ID=your_airtable_base_id_here

# REQUIRED: JWT Authentication Secret (generate a strong random string)
JWT_SECRET=your_super_secure_jwt_secret_minimum_32_characters_long

# REQUIRED: Admin Password Hash (use SHA-512 hash of your admin password)
ADMIN_PASSWORD_HASH=your_sha512_password_hash_here

# REQUIRED: Email Service Configuration
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=noreply@yourdomain.com
OWNER_EMAIL=your_business_email@yourdomain.com

# REQUIRED: SMS Service Configuration
CELLCAST_APPKEY=your_cellcast_app_key_here

# REQUIRED: Security Configuration
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# OPTIONAL: Deployment URL (auto-detected on Netlify)
URL=https://yourdomain.netlify.app

# OPTIONAL: Environment indicator
NODE_ENV=production

# Security Notes:
# 1. Never commit the actual .env file to version control
# 2. Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Generate ADMIN_PASSWORD_HASH with: node -e "console.log(require('crypto').createHash('sha512').update('your_password').digest('hex'))"
# 4. Use strong, unique passwords and API keys
# 5. Regularly rotate secrets, especially JWT_SECRET and ADMIN_PASSWORD_HASH